/**
 * react-native CLI config — bundles custom fonts (Plus Jakarta Sans +
 * JetBrains Mono) from ./assets/fonts. Android also reads them from
 * android/app/src/main/assets/fonts; run `npx react-native-asset` to (re)link.
 */
module.exports = {
  assets: ['./assets/fonts'],
};
